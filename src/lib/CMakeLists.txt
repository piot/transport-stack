# generated by cmake-generator
cmake_minimum_required(VERSION 3.16.3)

add_library(transport-stack STATIC 
  lower_level.c
  multi.c
  single.c)

include(Tornado.cmake)
set_tornado(transport-stack)

target_include_directories(transport-stack PUBLIC ../include)


target_link_libraries(transport-stack PUBLIC 
  udp-server
  udp-client
  udp-server-connections
  udp-connections-client
  hazy
  relay-client
  time-tick)

